{{#if loggedIn}}
  {{#if isListed}}
    <button class="btn" data-hull-action="remove">Remove {{target.name}} from {{list.name}}</button>
  {{else}}
    <button class="btn" data-hull-action="add">Add {{target.name}} to {{list.name}}</button>
  {{/if}}
{{else}}
  <div data-hull-component="login/button@hull"></div>
{{/if}}
