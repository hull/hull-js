define(['lib/hull'], function(Hull) {
  Hull({
    appId: 'YOUR_API_KEY',
    orgUrl: 'http://your.app.domain.tld',
    /*
    name: "name your app",
    debug: true,
    widgets: {
      sources: { 'default' : 'widgets/' }
    }
    */
  });
});

